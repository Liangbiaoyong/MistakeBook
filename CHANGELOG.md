# 变更记录

本项目的所有重要变更都记在这里。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [未发布]

### 变更
- **默认模型统一**：四个功能（采集识别 / 错因分析 / 变式出题 / 考点排行）开箱即指向同一个
  provider 与模型 —— 只需配一把 key 就能全跑通，不再出现「采集能用、其余三行报未配密钥」。
  默认 provider 为 OpenCode Go，模型名 `claude-sonnet-4-6`（见下）。**预置值只是默认，随时可改。**
  ⚠ 那个看着不相干的模型名是网关侧的别名：OpenCode Go **按模型名决定路由**，实测只有它会被路由到
  支持读图的模型；填 `gpt-4o`/`qwen-vl-max`/`glm-4v`/`deepseek-flash` 都会落到看不到图的模型上。
- **模型名称输入框被挤成一条缝**：根因是它用了 `flex-1`（展开为 `flex: 1 1 0%`，**基准宽度为 0**），
  同一行里又有很长的状态文字和两个按钮，空间不足时它被压到接近零宽。
  改为固定宽度 + `shrink-0`，并把所有状态/错误文字**移到行的下一行**（全宽可换行）。
- **新增「Provider 管理」区块（置于模型配置之上）**：把 provider 提升为一等公民。
  每行一览 `id · 显示名 · baseUrl · 协议格式徽章 · 密钥状态`，点开即可改
  baseUrl / 协议格式 / 附加请求头 / API Key。baseUrl 提供常用示例下拉，新建时预填协议默认值。
- **模型行的下一行摊开解析结果**：`⟶ <baseUrl> · <openai|anthropic> · 密钥状态`，
  让每个选择实际会打到哪儿一眼可见，不再靠猜。
- **厂商不再受下拉框限制**：模型配置里每行的 provider 从封闭 `<select>` 改为**自由输入 + 建议列表**。
  任意填 id 皆可，包括还不存在的；填了没配置的会在行内提示并可「补建这个 Provider」。
  **不做任何拦截**——存下来，调用失败时明确报错（这本来就是后端一直以来的行为，限制只在 UI）
- 「未知 provider」的报错文案改为可操作：说清去哪儿补、或换用已存在的
- 预置的 5 个 provider 明确标注为**示例**而非白名单

### 修复
- **假的原子写入**：原先写完 `.tmp` 立刻 `unlink`，再直接覆盖目标文件。改为真正的 `rename`，并确保目录存在
- **更新错题会重置复习进度**：保存时会无条件写回 `status: 'new'` 与 `review.round: 1`，等于每次评分都被抹掉（复习功能会完全失效）。改为按补丁合并，未提及的字段一律保留
- **id 碰撞会覆盖错题**：4 位 hex 只有 65536 种，2000 条必然碰撞。改为 6 位并加查重（`pickUniqueId`），把"查重"做成可确定性测试的纯函数
- **硬编码模型名**：vault 里写死了 `deepseek-flash`，用户换模型后会记录假数据。改为传入真实识别的模型名
- **两套冲突的复习间隔表**：vault 里另有一套 `1,3,7,14,30`，与 `review.ts` 的 `1,2,4,7,15,30` 不一致。统一收敛到 `review.ts`
- **`capture:start` 被注册两次**，异常中断了后续**所有** IPC 注册，应用变成空壳。已去除重复，并让 `handle()` 先 `removeHandler` 做幂等保护
- **索引其实在内存里**：`openIndex()` 用的是 `DatabaseSync(':memory:')`，与设计的「索引落盘到 `userData/index.db`」不符。改为文件索引
- **手搓的 YAML 解析器**：`frontmatter.ts` 自己解析而非用已装的 `gray-matter`，导致空数组变成 `['']`、数字被强转。改为 `gray-matter` 并补齐类型收窄
- **复习页拿不到题干和答案**：队列只有轻量摘要，页面却显示被截断的 `questionHead`。改为按 id 拉取完整记录并缓存
- **Tailwind v4 不允许 `@apply` 自定义类**：`.cu-btn-primary` 里的 `@apply cu-btn` 会让渲染层构建立即失败。改用共享选择器
- **思考模型会把输出预算吃光**：`max_tokens` 偏小时，返回里只有 `thinking` 块、没有 `text` 块，于是拿到空内容。客户端改为把传输层失败也纳入重试，并在错误里说清「返回了哪些块」
- **按热键没反应（框选界面永远不出现）**：两个 bug 叠在一起 ——
  ① `overlay.html` **没被打进构建产物**（`out/main/` 里只有 `index.js`），`loadFile` 直接 ENOENT；
  ② 即便文件在，`new URL('D:\\…\\overlay.html')` 在 Windows 路径上也会抛异常。
  改为用 electron-vite 的 `?asset` 引入，并把路径断言加进 `--selftest`
- **整屏截图走 URL 查询参数传递**：一张全屏图 base64 后有好几 MB，塞进 URL 会直接崩。改为落盘后只传文件路径，overlay 用 `file://` 读取，用完即删
- **框选时选区不凸显**：原先整张背景图统一压暗，选区自然也一起暗了，等于没起到引导作用。改为「压暗底图 + 选区处用原亮度的第二张图 clip 出来」，并加四角标记与实时像素尺寸
- **热键注册失败只写进控制台**：用户按了没反应却完全不知道原因。现在会弹出明确提示，并告知可用托盘菜单或去设置里换组合
- **没等背景图画好就显示窗口**：会先闪一下桌面。改为等 overlay 回发 `ready` 再显示（带 1.2s 兜底）

### 新增
- **托盘常驻 + 应用图标**：主窗口可有可无；关窗口改为收进托盘而非退出（要随时按热键）。
  托盘菜单：截图录入 / 打开主窗口 / 退出。图标为珊瑚红→鸢尾紫渐变圆角方块（与设计系统同源），
  应用图标用白色「错」，托盘图标用白色「✕」（16px 下汉字会糊，符号才看得清）
- **双协议支持**：除 OpenAI 兼容外，新增 **Anthropic 兼容**格式（`/v1/messages`）。`ProviderConfig`
  增加 `format` 与任意 `headers`，可对接任意厂商与网关
- **拉取模型列表**：设置页可向厂商请求可用模型并下拉选择；对方不暴露列表时明确提示手动填写
- **`--selftest` 自检模式**：不开窗口即可验证「配置 → 解密 Key → 协议层 → 文本/视觉」整条链路
- **OpenCode Go 预设**：内置本地网关（Anthropic 协议 + 必需的 `x-opencode-session` 头）
- 设置页新增「学习计划」区块（考试日期 + 全局热键，改热键会立刻重新注册）；provider 可见可编辑、可清除密钥
- 项目脚手架：Electron 44 + electron-vite 5 + React 19 + TypeScript 7 + Tailwind CSS 4
- 全屏英雄区（Fullscreen Hero）风格的设计系统：暗底、玻璃卡片、胶囊控件，含动效降级
- 共享类型与 IPC 契约（`src/shared/types.ts`、`src/shared/ipc.ts`）
- 带考试日期的复习调度器 `src/main/review.ts`，含 16 项单元测试
- 杂项设置存储 `src/main/settings.ts`（考试日期、热键）
- frontmatter 往返测试（覆盖 YAML 特殊字符、日期、空数组等易错场景）

### 新增
- **托盘常驻 + 应用图标**：主窗口可有可无；关窗口改为收进托盘而非退出（要随时按热键）。
  托盘菜单：截图录入 / 打开主窗口 / 退出。图标为珊瑚红→鸢尾紫渐变圆角方块（与设计系统同源），
  应用图标用白色「错」，托盘图标用白色「✕」（16px 下汉字会糊，符号才看得清）。

### 决策
- **选 Electron 而非 Tauri**：已有跑通的 Electron 构建经验，踩过的坑均有解；Tauri 的体积/内存优势对本项目不构成决策因素
- **SQLite 用 Electron 内置的 `node:sqlite`**：实测可用，换来零原生依赖，免掉 better-sqlite3 的 ABI 重编译
- **存储采用「文件为真相源 + SQLite 为派生索引」**：数据永远可由 Markdown 重建
- **不提供「押题」功能**：改写为「高频考点排行」，依据错题分布排优先级

[未发布]: https://github.com/Liangbiaoyong/MistakeBook/commits/main
