# 变更记录

本项目的所有重要变更都记在这里。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [未发布]

### 修复

**截屏与渲染**

- **公式在列表预览里退化成字面文字**。`questionHead` 由后端**按 50 个字符硬截**，正好切进 `$N_1$`
  里面 —— `$` 数量变成奇数、配对失败，渲染时整段公式就退化成带美元符号的普通文字。
  改为 `questionPreview()`：**截断点必须落在公式之外**（宁可少显示几个字）；同时在渲染器加兜底，
  落单的 `$` 直接去掉，免得露出一个突兀的美元符号。加 6 项单测逐一扫描各截断长度。
  同类问题在详情页 / 复习页 / 录入窗一并覆盖。
- **截图成功却被当成「取消」丢掉**（「截了图但没反应没结果」的真凶）。日志给出确凿现场：
  `截到 1920x1080` 之后 2.8 秒直接是「用户取消」，而用户并没有取消。根因是 `showOverlay` 里
  **先拆窗、后 resolve**：`cleanup()` 的 `destroy()` **会同步触发 `closed` 事件**（已用实验证实），
  而 `closed` 处理器也 `resolve(null)` —— Promise 只能 settle 一次，`null` 抢先落定。
  改为「先落定、再拆窗」，`closed` 在已落定时不再覆盖；结束原因分开记录。
- **拖完框松开鼠标后毫无反应**：overlay 只在按下回车时才提交，`mouseup` 里只把 `dragging` 置 false。
  改为**松开鼠标即提交**（所有截图工具的行为），保留回车提交、Esc/右键取消；
  选区过小时**不提交也不关闭**，让它重拖，不再把误点当成取消。
- **按热键没反应（框选界面永远不出现）**：两个 bug 叠在一起 —— ① `overlay.html` **没被打进构建产物**，
  `loadFile` 直接 ENOENT；② 即便文件在，`new URL('D:\\…')` 在 Windows 路径上也会抛异常。
  改用 electron-vite 的 `?asset`，并把路径断言加进 `--selftest`。
- **整屏截图走 URL 查询参数传递**：一张全屏图 base64 后好几 MB，塞进 URL 会直接崩。
  改为落盘后只传文件路径，overlay 用 `file://` 读取，用完即删。
- **框选时选区不凸显**：原先整张背景统一压暗，选区跟着一起暗，等于没起到引导作用。
  改为「压暗底图 + 选区处用原亮度图 clip 出来」，加四角标记与实时像素尺寸。
- **没等背景图画好就显示窗口**：会先闪一下桌面。改为等 overlay 回发 `ready` 再显示（带 1.2s 兜底）。
- **侧边栏底部的热键提示被窗口底边裁掉**：热键与句子挤在一行，换行后末行被切。改为热键单独占一行。

**打包与资源**

- **打包后任务栏 / 托盘图标是透明的**。图标原本用 `?asset` 引入，但 electron-vite 只把它解析成
  相对路径 `../../resources/icon.png` —— 开发模式下该位置存在，**打包后它在 app.asar 里并不存在**，
  而 `nativeImage.createFromPath()` 找不到文件时**不报错、只返回一张空图**。
  改为用 `process.resourcesPath` 显式解析 + electron-builder `extraResources` 带上 `resources/`。
- **Tailwind v4 不允许 `@apply` 自定义类**：`.cu-btn-primary` 里的 `@apply cu-btn` 会让渲染层构建立即失败。

**数据完整性**

- **假的原子写入**：原先写完 `.tmp` 立刻 `unlink`，再直接覆盖目标文件。改为真正的 `rename`，并确保目录存在。
- **更新错题会重置复习进度**：保存时无条件写回 `status: 'new'` 与 `review.round: 1`，等于每次评分都被抹掉
  （复习功能会完全失效）。改为按补丁合并，未提及的字段一律保留。
- **id 碰撞会覆盖错题**：4 位 hex 只有 65536 种，2000 条必然碰撞。改为 6 位并加查重（`pickUniqueId`），
  把「查重」做成可确定性测试的纯函数。
- **硬编码模型名**：vault 里写死了 `deepseek-flash`，用户换模型后会记录假数据。改为传入真实识别的模型名。
- **两套冲突的复习间隔表**：vault 里另有一套 `1,3,7,14,30`，与 `review.ts` 的 `1,2,4,7,15,30` 不一致。
  统一收敛到 `review.ts`。
- **手搓的 YAML 解析器**：`frontmatter.ts` 自己解析而非用已装的 `gray-matter`，导致空数组变成 `['']`、
  数字被强转。改为 `gray-matter` 并补齐类型收窄。
- **索引其实在内存里**：`openIndex()` 用的是 `DatabaseSync(':memory:')`，与设计的「索引落盘到
  `userData/index.db`」不符。改为文件索引。

**启动与 IPC**

- **`capture:start` 被注册两次**，异常中断了后续**所有** IPC 注册，应用变成空壳。
  已去除重复，并让 `handle()` 先 `removeHandler` 做幂等保护。
- **复习页拿不到题干和答案**：队列只有轻量摘要，页面却显示被截断的 `questionHead`。
  改为按 id 拉取完整记录并缓存。
- **热键注册失败只写进控制台**：用户按了没反应却完全不知道原因。现在会弹出明确提示。

**模型调用**

- **思考模型会把输出预算吃光**：`max_tokens` 偏小时返回里只有 `thinking` 块、没有 `text` 块，于是拿到空内容。
  客户端改为把传输层失败也纳入重试，并在错误里说清「返回了哪些块」。

### 新增

- **可观测性**（这轮最大的教训是「失败必须看得见」）：
  - 文件日志 `%APPDATA%\MistakeBook\mistakebook.log`（自动轮转，永不抛异常）。打包版没有控制台，
    此前所有诊断信息用户都看不到。`--selftest` 的结果、启动版本号、热键注册结果都写进去。
  - overlay 交互埋点（按下 / 松开 / 选区尺寸 / 提交 / 取消）经 IPC 转写进日志 ——
    下次「按了没反应」能直接看出用户到底做了什么。
  - 失败不再静默：截图流程出错会通过 toast 明确告知。
- **反馈动画**：提交时选区边框与四角闪白、确认胶囊缩放淡入；录入窗在识别期间有不确定进度条、
  字段骨架屏与分阶段状态文案（5–15 秒的等待不再是一片死寂）。全部在 `prefers-reduced-motion` 下降级。
- **托盘常驻 + 应用图标**：关窗口改为收进托盘而非退出（要随时按热键）。托盘菜单：截图录入 / 打开主窗口 / 退出。
  图标为珊瑚红→鸢尾紫渐变圆角方块（与设计系统同源），应用图标用白色「错」，托盘图标用白色「✕」
  （16px 下汉字会糊，符号才看得清）。
- **双协议支持**：除 OpenAI 兼容外，新增 **Anthropic 兼容**格式（`/v1/messages`）。
  `ProviderConfig` 增加 `format` 与任意 `headers`，可对接任意厂商与网关。
- **拉取模型列表**：设置页可向厂商请求可用模型并下拉选择；对方不暴露列表时明确提示手动填写。
- **`--selftest` 自检模式**：不开窗口即可验证「配置 → 解密 Key → 协议层 → 文本/视觉」整条链路，
  并检查框选界面 / 应用图标 / 托盘图标是否都在。
- 设置页新增「学习计划」区块（考试日期 + 全局热键，改热键会立刻重新注册）。
- 项目脚手架：Electron 44 + electron-vite 5 + React 19 + TypeScript 7 + Tailwind CSS 4。
- 全屏英雄区（Fullscreen Hero）风格的设计系统：暗底、玻璃卡片、胶囊控件，含动效降级。
- 共享类型与 IPC 契约（`src/shared/types.ts`、`src/shared/ipc.ts`）。
- 带考试日期的复习调度器 `src/main/review.ts`，含 16 项单元测试。
- 杂项设置存储 `src/main/settings.ts`（考试日期、热键）。

### 变更

- **默认模型统一**：四个功能开箱即指向同一个 provider 与模型 —— 只需配一把 key 就能全跑通。
  ⚠ 默认模型名 `claude-sonnet-4-6` 是网关侧的别名：OpenCode Go **按模型名决定路由**，
  实测只有它会被路由到支持读图的模型；填 `gpt-4o`/`qwen-vl-max`/`deepseek-flash` 都会落到看不到图的模型上。
  **预置值只是默认，随时可改。**
- **Provider 提升为一等公民**：新增「Provider 管理」区块置于模型配置之上，每行一览
  `id · 显示名 · baseUrl · 协议格式徽章 · 密钥状态`，点开即可改 baseUrl / 协议格式 / 附加请求头 / API Key。
- **模型行的下一行摊开解析结果**：`⟶ <baseUrl> · <openai|anthropic> · 密钥状态`，不再让人猜会打到哪儿。
- **厂商不再受下拉框限制**：provider 从封闭 `<select>` 改为自由输入 + 建议列表，任意填 id 皆可，
  填了没配置的会行内提示并可一键补建；**不做任何拦截**，调用失败时明确报错。
- **模型名称输入框被挤成一条缝**：根因是 `flex-1`（`flex: 1 1 0%`，基准宽度为 0），
  同行的长状态文字把它压到接近零宽。改为固定宽度 + `shrink-0`，状态文字移到下一行。
- 预置的 5 个 provider 明确标注为**示例**而非白名单。

### 文档

- README 增加界面截图（书库 / 识别中 / 识别完成 / 复习 / 统计 / 设置），
  并保留可复现的生成脚本 `scripts/screenshots.cjs`。

### 决策

- **选 Electron 而非 Tauri**：已有跑通的 Electron 构建经验，踩过的坑均有解；Tauri 的体积/内存优势对本项目不构成决策因素
- **SQLite 用 Electron 内置的 `node:sqlite`**：实测可用，换来零原生依赖，免掉 better-sqlite3 的 ABI 重编译
- **存储采用「文件为真相源 + SQLite 为派生索引」**：数据永远可由 Markdown 重建
- **不提供「押题」功能**：改写为「高频考点排行」，依据错题分布排优先级

[未发布]: https://github.com/Liangbiaoyong/MistakeBook/commits/main
