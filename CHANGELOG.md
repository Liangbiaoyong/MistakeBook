# 变更记录

本项目的所有重要变更都记在这里。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [0.2.5] - 2026-09-14

第一个公开发布版。更早的 0.2.x 只作为 git 提交存在过，从未对外发布 —— 所以下面这一整段
（项目从零到 0.2.5 的全部变更）都归在这一版里。

### 修复

**复习页：书库里有题，它却说「还没有错题」，而且连自己选范围的地方都没有**

用户报的原话是「书库有题目，但复习那里显示没有题目，虽然所有题目我都做过了，但是还是不能自定义以及重做」。

- **根因是「先查询、后恢复偏好」这个顺序**。原来的流程是：挂载 → 用默认值（`mode:'due'`）查一次 →
  异步读到持久化偏好，把 `mode` 改成 `all` → **但不重查**。于是屏幕上是 `due` 的结果（0 条）、
  控件里是 `all` 的状态，空态照着 `all` 选分支，落到「还没有错题 / 开始录入错题吧」——
  对着一个有几百道错题的人说这句话。而且那一支**没有任何按钮**。
  证据在日志里一目了然：`复习查询：范围 {} · due · due · 命中 0` 重复了十几次，
  **一次 `all` 都没有**，因为恢复偏好从来不触发查询。
  改为**先恢复偏好、再查一次**（引导阶段用局部取值构造查询，再一并写回状态），
  并给偏好写回加了「引导完成前不写」的闸 —— 否则挂载那一轮的默认值会先把用户偏好冲掉。
- **筛选栏（范围 / 模式 / 顺序 / 批次）只写在「有题」那一支的 return 里**，其它分支各自 return 一整个页面，
  所以任何空态下都点不到「开始复习」—— 这就是「不能自定义」。改为统一的 `shell()` 骨架，
  **筛选栏任何状态下都在**。
- **空态分不清「一道题都没录」和「都复习过了、今天没到期」**。`ReviewBatch` 增加
  `libraryTotal` / `librarySubjects`（两次极便宜的 COUNT / DISTINCT），空态据此说话：
  库里真是空的才说「还没有错题」，否则说「今天没有到期的错题，库里共 N 道」并给出
  「切到「全部范围内」」的出路。
- **「再做一遍这批」一重启就没了**：`lastBatchIds` 只活在内存里，而它最该起作用的时候恰恰是
  「练完一轮、隔天想回顾」——那时它已经没了，页面还因为没有到期题而空着。改为随偏好持久化。
- **同一页的两处死控件**：题型的 `<select>` 有 state、有 `hasFilters` 判断，却**从没进过查询**
  （`ListFilter` 里根本没有 `type` 字段）；科目下拉的选项**永远是空的**，只有「全部科目」一项、
  等于选不了。前者把 `type` 补进 `ListFilter` → SQL → `queryFrom`；后者改用书库真实科目填充。
- **回归检查**：新增 `scripts/review-empty-check.cjs`，三个场景七条断言钉住上面这些
  （含「恢复偏好后第一次查询用的就是恢复出来的模式」，修复前这条会红）。

**截图：框选界面盖不住 Windows 任务栏，不是真的全屏**

用户报「截图的图包括了任务栏，全屏展示时任务栏依旧还在，做不到完全的全屏」。

- 桌面通知和整屏截图本身**包含**任务栏是对的（截图工具都这样，你也可能确实想截它），
  问题在于 overlay 窗口**盖不住**它：底部那一条仍是**实时的**任务栏，既不能框选，看着也不像全屏。
- 用像素量出来的结论（把 overlay 铺成纯色再抓屏、读屏幕底部那条）：
  `bounds + alwaysOnTop(true)` ✗ ／ `bounds + alwaysOnTop('screen-saver')` ✗（Windows 上 level 不起作用）／
  **`fullscreen: true` + `transparent: true` ✓**。给 overlay 窗口加上 `fullscreen: true`。
  这次不靠肉眼判断，量的是底部 12px 的平均色。

**截图之后什么都没有：任务卡在「正在识别」，自动保存从来没启动过**（新加的截图断言抓出来的）

- `App.tsx` 的 `handleCaptured` 把 `applyExtractionResult()` **塞进了 `setTasks` 的 updater 里**。
  updater 里再调 setState 是渲染期更新，结果被丢掉 —— 任务永远停在「正在识别」，
  30 秒倒计时**根本不会开始**，`已保存 ✓` 也不会出现。
  而 `notifySync(tasks)` 推给桌面通知窗的就是这份卡住的状态，于是软件内浮层和桌面通知**一起卡**。
  改为先算出 task id、在 updater 之外应用结果；「同一张图不处理两次」的去重也从 updater 里
  挪到 `tasksRef` —— 同一瞬间到达的两张截图本来会在 React 提交前都读到旧 state，去重形同虚设。
- **为什么这么久没人发现**：`scripts/screenshots.cjs` 里「识别完成」和「录入窗」两张图
  **一直没有断言**，而它们从那次改动起截的就只是书库页 —— README 上挂的正是这两张错的图。
  现在两处都加了断言：浮层必须有「编辑」+ 倒计时文本，录入窗必须出现查重提示且无残留 `$`。
  截图脚本本身也有个坑：步骤之间靠点「丢弃」清任务，而**「识别中」的卡片只有进度条、没有丢弃按钮**，
  于是第二个 capture 被去重挡掉、后面全废 —— 改用 `reload()` 清空渲染层再继续。
- **查重面板与录入窗的题干当纯文本渲染**（`$n$` 原样露出美元符号）。这是本项目已经修过一次的
  同一类错误（`MistakeCard` 的老问题）：`questionHead` 是 Markdown 片段，必须走 `<Markdown>`。
  两处都改掉，并加进截图断言（`残留 $ == 0`）。

### 新增

- **时间维度：复习事件日志 + 周期对比**。统计原先全是「有史以来」，而备考后期你更需要知道
  「这周比上周好了还是差了」。
  - 每次评分往 vault 根的 **`reviews.jsonl` 追加一行**（`{id, date, grade, round}`）。
    **放在 vault 里而不是索引里**：索引可以从 Markdown 重建，而复习历史重建不出来 ——
    Markdown 只留得下「上次复习」和「轮次」，中间过程一旦覆盖就没了。
    用 JSONL 是因为追加只写一行、不重写整个文件，断电最多毁掉最后一行。
  - 索引新增 `reviews` 表（派生），`rebuildIndex()` 由日志回填 —— 否则重建一次趋势就全成 0。
  - 统计页新增「近 N 天 vs 前 N 天」：新增错题、复习次数、**遗忘率**（`忘了` 次数 / 复习次数），
    统一成一条规则 —— **少错 / 多练 / 少忘 = 进步，标绿**。
  - 每日曲线从「只有新增」变成**新增 + 复习两条线**，并**补齐没有数据的日子**：
    SQL 的 `GROUP BY` 只返回有数据的天，直接画折线会让空日被邻居跨过去，看着像那天有量。
  - ⚠ 复习日志是这一版才有的：还没有任何记录时，界面**明说「还没来得及统计」**，
    而不是显示成「复习 0 次」（那会被读成「我一道都没复习」）。
  - `src/main/store/trend.ts` 纯函数（日期算术 / 窗口切分 / 补齐），23 项单测 —
    这部分最容易出的是差一天、漏一天、两个窗口重叠这类静默错误。
- **错题查重与合并**。同一道题截两次会存两条，而两条会各自进复习队列、各自算进统计 ——
  「这个考点我到底错过几次」直接虚高。
  - `src/main/store/dedup.ts` 纯函数：归一化（去空白 / LaTeX 定界符 / 全角半角标点）+ 字符二元组
    Jaccard。阈值 0.75 —— 一道 20 来字的题错一个字相似度就掉到 0.8 出头，定太高会漏掉
    「同一道题、识别稍差」这种最常见的情况；而无关的中文题通常只有 0.1 上下，离阈值很远。
    短题干（归一化后不足 8 字）只认完全相同：二元组在短串上噪声太大，宁可漏报也不要误报。
  - **保存时提示**「书库里已经有 N 道很像的题」，可选**并入**而不是又存一条；
    并入是**补空缺**而不是覆盖（已有的内容是你可能手改过的），复习进度原样保留。
  - **书库整体查重**：并查集分组，默认保留**复习轮次最多**的那条 —— 进度是最攒出来的东西，
    留错一条就白复习了。被并掉的移入 `.trash/`（不是删除），并记进 `merged_from`。
  - 21 项单测（含「A≈B、B≈C 应在同一组」、「不跨科目比较」、「空题干不算重复」）。
- **`merged_from` frontmatter 键**：合并掉的是记录，不是历史 —— 「这道题其实错过三次」不该消失。
- **截图脚本新增两张断言过的图**：`library-dedup.png`（查重面板）与录入窗的查重提示，
  README 同步。

### 变更

- **`statsWindowDays` 从死字段变成真的开关**。它一直躺在 `AppSettings` 的默认值里，
  但 `statsOverview()` 把窗口**硬编码成 7 天**、设置页里根本没有这个开关 —— 又一处
  「看起来有的东西，实际没接上」。现在统计页的趋势与曲线都按它算，
  设置 → 学习计划里可改（1–365 天，附 7 / 14 / 30 / 90 快捷键）。

### 修复

**人性化审计（两轮 agent 审查约 25 条 + 我逐条核实后修复）**

- **索引损坏会让应用整个起不来**，而且设置页那个「重建索引」按钮**走同一条死路** ——
  设计文档承诺的「索引损坏 → 一键从 MD 重建」在代码里根本不存在。
  现在 `openIndex()` 自愈：损坏时把文件改名为 `index.db.bad-<时间戳>`（**从不删除**）→ 新建 → 自动重建。
- **库目录不存在时静默回退到默认目录**：移动硬盘没挂上 → 看到空书库（以为数据没了）→
  接着截图就**写进另一个库，从此分裂成两个错题本**。改为报错并**停止写入**，新增 `getVaultStatus()`。
- **删除是真删**，且 `.md` 删除失败被吞掉、**原图先没了**（残留 .md 指向不存在的截图）。
  改为移入 `.trash/<id>/`，索引扫描跳过 `.trash`。
- **手改过的 Markdown 会被静默削掉**：用户自己写的 `## 我的备注` 或自定义 frontmatter 键，
  在应用里改一下科目就没了 —— 而「文件是唯一真相源、可手改」是这软件的核心卖点。
  现在未知小节与未知键**原样保留**（含往返测试）。
- **低置信度结果被自动保存**：设计与文档都写明「低于 0.75 强制人工核对」，实际只实现在 Composer 里 ——
  手写题被识别成幻觉题干，30 秒后静默入库。现在 <0.75 **不启动倒计时**，通知卡明确写「置信度 62%，请核对」。
- **没有单实例锁**：第二次启动会得到第二个托盘图标、第二次注册热键失败弹窗，且两个进程同时写同一个索引与 vault。
- **连按两次热键 → 同一张图被识别两次、存两条重复错题**。
- **截图失败时用户什么都看不到**：提示发给主窗口的 Toast，而主窗口通常收在托盘里。改走桌面通知窗。
- **改了热键后界面和托盘提示还在说旧热键**。
- **没有开机自启**：软件常态是常驻托盘，但每次重启都得手动启动一次，否则热键是死的。新增开关，**默认关**。
- **所有弹窗都没有 Esc、没有焦点陷阱**：Esc 关不掉，Tab 会一路走到背后的侧边栏和卡片上；表单也不吃 Ctrl+Enter。
- **复习页不能键盘评分**：一天上百道题只能鼠标点。现在 Space/Enter 显答案、`1`–`4` 评分、`←`/Backspace 回上一题。
- **搜索只搜题干**：`q` 不含我的思路/解法/错因/知识点/来源，`chapter` 是全等匹配，`point` 根本没有调用点 ——
  直接击穿「考前定向复习」。改为多字段 LIKE + 子串匹配。
- **搜索每敲一个字整个列表闪成 Spinner**：改为 250ms 防抖 + 刷新时保留旧列表 + 序列号丢弃过期响应。
- **筛选到 0 条时显示「还没有错题」**：现在区分「一道都没有」与「当前筛选无结果」（后者给「清除筛选」）。
- **统计页「待复习」与复习页互相矛盾**：`date('now')` 是 UTC，而 `review.next` 是本地日期 ——
  本地 00:00–08:00 之间统计显示 0 而复习页有题。每日错题量同样按 UTC 分桶。统一用本地日期。
- **保存后没有任何撤销路径**：已保存卡片停留时间延长并给「撤销」。
- **书库把全部条目一次性塞进 DOM**（2000 题会卡）：加 `content-visibility: auto`。
- **通知的进度条分母写死 30**（设成 60 会溢出 200%）；**通知只出现在主显示器**（PDF 放副屏就看不到识别结果）。
- **界面里硬编码热键提示**；**错误信息无法直达设置页**。

**截屏与渲染**

- **列表卡片里的公式从来没渲染过**（用户反复报的「公式显示有问题」）。真因是 `MistakeCard`
  把 `questionHead` 当**纯文本**渲染，压根没走 Markdown 组件 —— 于是 `$...$` 原样显示成美元符号。
  当初绕开渲染器，是因为卡片根节点是 `<button>`（内容模型只允许行内内容），而 Markdown 组件输出 `<div>`，
  两者不兼容。改为 `role="button"` 的 `div` + 键盘处理，预览走 `<Markdown>`。
  ⚠ 上一版修的「截断点落在公式中间」是**另一个真实但不同的缺陷**，并没有治到这个症状 —— 记在这里以免混淆。
- **给这类问题加了一道断言**：README 截图脚本现在会检查书库页「KaTeX 公式数 > 0 且残留 `$` = 0」，
  公式没渲染就直接失败退出。上一轮我是靠肉眼看低分辨率截图判断的，**看错了** —— 这种事不该再靠眼睛。
- **公式在列表预览里被截断**。`questionHead` 由后端按 50 个字符硬截，会切进 `$N_1$` 里面，
  使 `$` 数量变成奇数、配对失败，渲染时整段公式退化成字面文字。改为 `questionPreview()`：
  截断点必须落在公式之外（宁可少显示几个字）；渲染器同时加兜底，落单的 `$` 直接去掉。
  加 6 项单测逐一扫描各截断长度。同类处理在详情页 / 复习页 / 录入窗一并覆盖。
- **桌面通知里的知识点永远是空的**：`notify.html` 读的是 `extraction.knowledgePoints`，
  而真实类型是 `points` —— 字段名对不上，那一段永远是空白。已修，并在回归脚本里专门断言这一项。
- **「已保存 ✓」永远不会显示**：`notify-window.ts` 与 `notify.html` **两处**都把 `saved`
  从「活跃任务」里过滤掉了，于是保存成功后窗口直接消失 —— 而这恰恰是用户最想确认的一步。
- **通知窗固定 170px 高会把第二张卡裁掉**：连截几张图时后面的看不见。改为内容报高度、
  主进程调整窗口（右下角锚定不变）。
- **通知的白玻璃压在白色 PDF 上等于看不见**：卡片原是 `rgba(255,255,255,0.10)` + 白字，
  而你读的 PDF 是白底。改为深色近乎不透明底 —— 桌面通知必须在**任何背景**上都可读。
- **截图时会把主窗口硬拽到前台**：`doCapture()` 里有一句 `mainWindow.show()`，
  直接毁掉「不打开软件也能截图」这个需求。已去掉，反馈交给桌面通知窗。
- **详情浮层被整页扫描图占满**：原始截图不设高度限制，一张整页扫描件就把 693px 的面板高度吃光，
  于是 chips、题目、答案、解析**全在折叠线以下**，得先滚过一整页图才看得到真正要看的东西。
  改为默认只露顶部一截（`max-h-52 object-cover object-top` + 渐隐提示），点击展开原图。
- **窗口收进托盘后，自动保存倒计时会卡住**：隐藏窗口默认被浏览器节流（定时器降到约每分钟一次），
  而识别与自动保存的流水线跑在渲染层。给主窗口关掉 `backgroundThrottling`。
- **截图成功却被当成「取消」丢掉**（「截了图但没反应没结果」的真凶）。日志给出确凿现场：
  `截到 1920x1080` 之后 2.8 秒直接是「用户取消」，而用户并没有取消。根因是 `showOverlay` 里
  **先拆窗、后 resolve**：`cleanup()` 的 `destroy()` **会同步触发 `closed` 事件**（已用实验证实），
  而 `closed` 处理器也 `resolve(null)` —— Promise 只能 settle 一次，`null` 抢先落定。
  改为「先落定、再拆窗」，`closed` 在已落定时不再覆盖；结束原因分开记录。
- **拖完框松开鼠标后毫无反应**：overlay 只在按下回车时才提交，`mouseup` 里只把 `dragging` 置 false。
  改为**松开鼠标即提交**（所有截图工具的行为），保留回车提交、Esc/右键取消；
  选区过小时**不提交也不关闭**，让它重拖，不再把误点当成取消。
- **按热键没反应（框选界面永远不出现）**：两个 bug 叠在一起 —— ① `overlay.html` **没被打进构建产物**，
  `loadFile` 直接 ENOENT；② 即便文件在，`new URL('D:\\…')` 在 Windows 路径上也会抛异常。
  改用 electron-vite 的 `?asset`，并把路径断言加进 `--selftest`。
- **整屏截图走 URL 查询参数传递**：一张全屏图 base64 后好几 MB，塞进 URL 会直接崩。
  改为落盘后只传文件路径，overlay 用 `file://` 读取，用完即删。
- **框选时选区不凸显**：原先整张背景统一压暗，选区跟着一起暗，等于没起到引导作用。
  改为「压暗底图 + 选区处用原亮度图 clip 出来」，加四角标记与实时像素尺寸。
- **没等背景图画好就显示窗口**：会先闪一下桌面。改为等 overlay 回发 `ready` 再显示（带 1.2s 兜底）。
- **侧边栏底部的热键提示被窗口底边裁掉**：热键与句子挤在一行，换行后末行被切。改为热键单独占一行。

**打包与资源**

- **打包后任务栏 / 托盘图标是透明的**。图标原本用 `?asset` 引入，但 electron-vite 只把它解析成
  相对路径 `../../resources/icon.png` —— 开发模式下该位置存在，**打包后它在 app.asar 里并不存在**，
  而 `nativeImage.createFromPath()` 找不到文件时**不报错、只返回一张空图**。
  改为用 `process.resourcesPath` 显式解析 + electron-builder `extraResources` 带上 `resources/`。
- **Tailwind v4 不允许 `@apply` 自定义类**：`.cu-btn-primary` 里的 `@apply cu-btn` 会让渲染层构建立即失败。

**数据完整性**

- **假的原子写入**：原先写完 `.tmp` 立刻 `unlink`，再直接覆盖目标文件。改为真正的 `rename`，并确保目录存在。
- **更新错题会重置复习进度**：保存时无条件写回 `status: 'new'` 与 `review.round: 1`，等于每次评分都被抹掉
  （复习功能会完全失效）。改为按补丁合并，未提及的字段一律保留。
- **id 碰撞会覆盖错题**：4 位 hex 只有 65536 种，2000 条必然碰撞。改为 6 位并加查重（`pickUniqueId`），
  把「查重」做成可确定性测试的纯函数。
- **硬编码模型名**：vault 里写死了 `deepseek-flash`，用户换模型后会记录假数据。改为传入真实识别的模型名。
- **两套冲突的复习间隔表**：vault 里另有一套 `1,3,7,14,30`，与 `review.ts` 的 `1,2,4,7,15,30` 不一致。
  统一收敛到 `review.ts`。
- **手搓的 YAML 解析器**：`frontmatter.ts` 自己解析而非用已装的 `gray-matter`，导致空数组变成 `['']`、
  数字被强转。改为 `gray-matter` 并补齐类型收窄。
- **索引其实在内存里**：`openIndex()` 用的是 `DatabaseSync(':memory:')`，与设计的「索引落盘到
  `userData/index.db`」不符。改为文件索引。

**启动与 IPC**

- **`capture:start` 被注册两次**，异常中断了后续**所有** IPC 注册，应用变成空壳。
  已去除重复，并让 `handle()` 先 `removeHandler` 做幂等保护。
- **复习页拿不到题干和答案**：队列只有轻量摘要，页面却显示被截断的 `questionHead`。
  改为按 id 拉取完整记录并缓存。
- **热键注册失败只写进控制台**：用户按了没反应却完全不知道原因。现在会弹出明确提示。

**模型调用**

- **思考模型会把输出预算吃光**：`max_tokens` 偏小时返回里只有 `thinking` 块、没有 `text` 块，于是拿到空内容。
  客户端改为把传输层失败也纳入重试，并在错误里说清「返回了哪些块」。

### 修复

- **考点分析的结果不留档**：结果只活在组件 state 里，切走页面就没了 —— 而分析一次要跑十几秒、
  还消耗 token，「上次说了什么」本来就是复习时要对着看的。现在每次成功分析**自动留档到
  `<vault>/analyses/<时间戳>.md`**（frontmatter 记时间与参与分析的错题数），
  界面显示**分析时间**并提供**历史记录**（可回看、可删）。错因归纳同样留档。
  自检里加了写入→读回→列表→清理的往返检查。
- **复习做完后显示 0 题、且不让重做**：完成态的「重新开始」重查的是**到期队列**，
  而那批题**刚被评过分**、`next` 已推到未来 → 返回 0 条 → 掉进"范围内没有到期的错题"，
  而且没有回头路。新增 `ReviewQuery.ids`（**按给定 id 顺序取题，不受到期日影响**），
  完成态与空态都提供「**再做一遍这批**」，并把重做做成正常操作而不是错误兜底。
- **`--selftest` 在应用已运行时静默退出**：单实例锁把诊断命令也挡掉了，什么都不打印 ——
  看起来就像"自检坏了"。现在 `--selftest` 豁免单实例锁。
- **自检输出被打两遍**（`say` 既 console.log 又 logLine，而后者会镜像到控制台）。

### 修复

- **`status`（未复习 / 复习中 / 已掌握）是个死字段**：全项目**没有任何地方会自动推进它** ——
  所有 `setStatus` 命中都是筛选控件，没有一处是错题自己的状态。于是每道题永远停在「未复习」，
  统计页的「**已掌握**」永远是 0，按状态筛选也筛不出东西 —— 而它本该是「我到底掌握了多少」的唯一答案。
  新增 `deriveStatus()` 让状态跟着复习历史走：顺利复习到第 4 轮 → **已掌握**；
  答「忘了 / 困难」→ **退回复习中**（不论之前多熟，轮次本身也已回退）。评分反馈里把状态推进也说出来。
  含 3 项单测（含「已掌握的题答错会退回去」）。

### 新增

- **闭环四件**（把已经做好的能力，接到「实际去用」那一步）：
  - **评分后显示「下次复习：9月16日」**。原先评完分不知道它什么时候会回来 —— 调度是黑箱，
    人就不会信任它。现在 `grade()` 返回新的复习状态并如实显示（当天则是「今天还要再看一遍」）。
  - **统计页可点进复习**：按错因与最常错知识点的条目可点击，直接跳到复习页并预设该范围。
    原先统计只告诉你「你错在概念混淆」，却点不进去。
  - **复习时生成变式**：变式入口原先只在书库详情里，而复习那一刻才是最想「再来一道同类」的时候。
  - **书库导出当前筛选为 Markdown**：课前打印（尤其数学）或换设备翻，导出的是当前筛选那一批。
- **从考点直接去复习**：考点排行每行现在有「复习这个考点」按钮，一键跳到复习页并预设该知识点
  （原先把"最该补的考点"和"实际去练"之间是断的 —— 你得自己记住考点名再去手输）。
- **复习会话（可指定范围与参数）**。原先「换一批」根本没法用——`due()` 返回**全部**到期项，
  按钮只是重查同一批并跳回第 1 题：**根本没有"批"的概念**。现在引入了显式的批次：
  - **范围**：科目 / 章节或知识点 / 错因 / 题型 / 状态 / 只看有原图
  - **模式**：只过到期的（走间隔重复调度）或 忽略到期日主动刷某个范围
  - **顺序**：按到期 / 随机 / 按录入时间 / 按难度
  - **批次**：10 / 20 / 50 / 全部；「换一批」= `offset += limit` 真正取下一批，
    转到尾会**绕回开头**并提示「已从头开始」（而不是给一片空白）
  - **偏好持久化**：下次打开复习页保持上次的范围
  - 范围内有题但没有到期的，会明确说明并给**一键切到「全部范围内」**
- **回归检查脚本**：`scripts/notify-check.cjs`（通知窗四状态断言）、
  `scripts/review-check.cjs`（断言「换一批」真的换了题、绕回提示、键盘评分）。
  两个都是"同一文件被多轮改动"最易出问题的地方，用断言而不是肉眼看图。
- **桌面级独立通知窗**（不需要打开软件）：屏幕右下角一张**不抢焦点**的卡片，
  显示识别进度 → `N 秒后自动保存` 倒计时 → `已保存 ✓`，带 保存 / 编辑 / 丢弃 按钮；
  点「编辑」才把主窗口唤出来。窗口常驻复用，有任务才显示。
- **后台流水线**：截图后不再弹阻塞式窗口，识别与自动保存全在后台跑（默认 30 秒，
  可在「设置 → 学习计划」改，填 0 表示不自动保存）。软件内也有同样的浮层。
- **可观测性**（这轮最大的教训是「失败必须看得见」）：
  - 文件日志 `%APPDATA%\MistakeBook\mistakebook.log`（自动轮转，永不抛异常）。打包版没有控制台，
    此前所有诊断信息用户都看不到。`--selftest` 的结果、启动版本号、热键注册结果都写进去。
  - overlay 交互埋点（按下 / 松开 / 选区尺寸 / 提交 / 取消）经 IPC 转写进日志 ——
    下次「按了没反应」能直接看出用户到底做了什么。
  - 失败不再静默：截图流程出错会通过 toast 明确告知。
- **反馈动画**：提交时选区边框与四角闪白、确认胶囊缩放淡入；录入窗在识别期间有不确定进度条、
  字段骨架屏与分阶段状态文案（5–15 秒的等待不再是一片死寂）。全部在 `prefers-reduced-motion` 下降级。
- **托盘常驻 + 应用图标**：关窗口改为收进托盘而非退出（要随时按热键）。托盘菜单：截图录入 / 打开主窗口 / 退出。
  图标为珊瑚红→鸢尾紫渐变圆角方块（与设计系统同源），应用图标用白色「错」，托盘图标用白色「✕」
  （16px 下汉字会糊，符号才看得清）。
- **双协议支持**：除 OpenAI 兼容外，新增 **Anthropic 兼容**格式（`/v1/messages`）。
  `ProviderConfig` 增加 `format` 与任意 `headers`，可对接任意厂商与网关。
- **拉取模型列表**：设置页可向厂商请求可用模型并下拉选择；对方不暴露列表时明确提示手动填写。
- **`--selftest` 自检模式**：不开窗口即可验证「配置 → 解密 Key → 协议层 → 文本/视觉」整条链路，
  并检查框选界面 / 应用图标 / 托盘图标是否都在。
- 设置页新增「学习计划」区块（考试日期 + 全局热键，改热键会立刻重新注册）。
- 项目脚手架：Electron 44 + electron-vite 5 + React 19 + TypeScript 7 + Tailwind CSS 4。
- 全屏英雄区（Fullscreen Hero）风格的设计系统：暗底、玻璃卡片、胶囊控件，含动效降级。
- 共享类型与 IPC 契约（`src/shared/types.ts`、`src/shared/ipc.ts`）。
- 带考试日期的复习调度器 `src/main/review.ts`，含 16 项单元测试。
- 杂项设置存储 `src/main/settings.ts`（考试日期、热键）。

### 变更

- **默认模型统一**：四个功能开箱即指向同一个 provider 与模型 —— 只需配一把 key 就能全跑通。
  ⚠ 默认模型名 `claude-sonnet-4-6` 是网关侧的别名：OpenCode Go **按模型名决定路由**，
  实测只有它会被路由到支持读图的模型；填 `gpt-4o`/`qwen-vl-max`/`deepseek-flash` 都会落到看不到图的模型上。
  **预置值只是默认，随时可改。**
- **Provider 提升为一等公民**：新增「Provider 管理」区块置于模型配置之上，每行一览
  `id · 显示名 · baseUrl · 协议格式徽章 · 密钥状态`，点开即可改 baseUrl / 协议格式 / 附加请求头 / API Key。
- **模型行的下一行摊开解析结果**：`⟶ <baseUrl> · <openai|anthropic> · 密钥状态`，不再让人猜会打到哪儿。
- **厂商不再受下拉框限制**：provider 从封闭 `<select>` 改为自由输入 + 建议列表，任意填 id 皆可，
  填了没配置的会行内提示并可一键补建；**不做任何拦截**，调用失败时明确报错。
- **模型名称输入框被挤成一条缝**：根因是 `flex-1`（`flex: 1 1 0%`，基准宽度为 0），
  同行的长状态文字把它压到接近零宽。改为固定宽度 + `shrink-0`，状态文字移到下一行。
- 预置的 5 个 provider 明确标注为**示例**而非白名单。

### 文档

- README 增加界面截图（书库 / 识别中 / 识别完成 / 复习 / 统计 / 设置），
  并保留可复现的生成脚本 `scripts/screenshots.cjs`。

### 决策

- **选 Electron 而非 Tauri**：已有跑通的 Electron 构建经验，踩过的坑均有解；Tauri 的体积/内存优势对本项目不构成决策因素
- **SQLite 用 Electron 内置的 `node:sqlite`**：实测可用，换来零原生依赖，免掉 better-sqlite3 的 ABI 重编译
- **存储采用「文件为真相源 + SQLite 为派生索引」**：数据永远可由 Markdown 重建
- **不提供「押题」功能**：改写为「高频考点排行」，依据错题分布排优先级

[0.2.5]: https://github.com/Liangbiaoyong/MistakeBook/releases/tag/v0.2.5
